# Dependencies
node_modules/

# Build output (regenerated via `npm run build`)
# The *.min.js files ARE checked in so static hosts work without a build step,
# but they should be regenerated by running `npm run build` whenever their
# source changes. Re-run the build before committing changes to .js files.
# (Not gitignored — they ship to the user.)

# OS/editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# Local server logs
*.log
server/.env
.vercel
.env*
