{
  "name": "advisoros-ai-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal backend proxy so AdvisorOS can call the Anthropic API without exposing the API key in the browser.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
